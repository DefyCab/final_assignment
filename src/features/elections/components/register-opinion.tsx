import type { Election } from "../repository";

type Props = {
  elections: Election[];
};

export function RegisterOpinion({ elections }: Props) {
  return (
    <div>
      <article></article>
    </div>
  );
}
